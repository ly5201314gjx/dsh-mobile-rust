[package]
name = "dsh-link"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "DSH Link 配对协议：Windows 守护进程与安卓客户端之间的配对密钥、链接编码与通道报文（纯 Rust 可单测）"

[dependencies]
rand = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
default = []