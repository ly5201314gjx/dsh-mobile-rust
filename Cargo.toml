[workspace]
resolver = "2"
members = ["core", "app", "tool"]

[workspace.package]
version = "3.0.0"
edition = "2021"
license = "MIT"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
panic = "unwind"
